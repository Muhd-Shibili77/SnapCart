<%- include ('../partial/header')-%>
    <style>
        .size-unavailable {
            text-decoration: line-through;
            opacity: 0.5;
            pointer-events: none;
        }

        .image-container1 {
            width: 100%;
            height: 51.5vh;
            overflow: hidden;
            position: relative;
        }

        .image-container2 {
            width: 100%;
            height: 51.5vh;
            overflow: hidden;
            position: relative;
        }

        .image-container3 {
            width: 100%;
            height: 51.5vh;
            overflow: hidden;
            position: relative;
        }

        .image-container4 {
            width: 100%;
            height: 51.5vh;
            overflow: hidden;
            position: relative;
        }

        .productImage {
            width: 100%;
            height: auto;
            transition: transform 0.3s ease, transform-origin 0.3s ease;
            cursor: pointer;
            margin-bottom: 80px;
        }

        .image-container1:hover .productImage {
            transform: scale(1.5);
            /* Zoom in the image */
        }

        .image-container2:hover .productImage {
            transform: scale(1.5);
            /* Zoom in the image */
        }

        .image-container3:hover .productImage {
            transform: scale(1.5);
            /* Zoom in the image */
        }

        .image-container4:hover .productImage {
            transform: scale(1.5);
            /* Zoom in the image */
        }
    </style>
    <!-- ***** Header Area Start ***** -->
    <header class="header-area header-sticky">
        <div class="container">
            <div class="row">
                <div class="col-12">
                    <nav class="main-nav">
                        <!-- ***** Logo Start ***** -->
                        <a href="#" class="logo">
                            <img src="/images/SnapCart.png">
                        </a>
                        <!-- ***** Logo End ***** -->
                        <!-- ***** Menu Start ***** -->
                        <ul class="nav">
                           
                            <li class="scroll-to-section"><a href="/user/home">Home</a></li>
                            <li class="scroll-to-section"><a href="/user/home">Men's</a></li>
                            <li class="scroll-to-section"><a href="/user/home">Women's</a></li>
                            <li class="scroll-to-section"><a href="/user/home">Kid's</a></li>
                            <li class="scroll-to-section"><a href="/user/products">Products</a></li>
                            <li class="scroll-to-section">
                                <a href="/cart" class="d-flex align-items-center">
                                    <i class="bi bi-cart"></i>
                                    <!-- Badge for Cart Count next to the cart icon -->
                                    <span class="badge badge-pill badge-secondary" style=" width: 25px; height: 25px; border-radius: 50%; display: flex; align-items: center; justify-content: center;">
                                        <%=cartCount%>
                                    </span>
                                </a>
                            </li>
                            <li class="scroll-to-section"><a href="/wishlist"><i class="bi bi-heart"></i></a></li>
                            <li class="submenu">
                                <a href="javascript:;"><i class="bi bi-person-circle"></i></a>
                                <ul>
                                    <li><a href="/user/profile">My Account</a></li>
                                    <li><a href="/order/order_history">My Orders</a></li>
                                    <li><a href="/wallet">My Wallet</a></li>
                                    <li><a href="/user/about">About Us</a></li>
                                    <li><a href="/user/contact">Contact Us</a></li>
                                    <li><a href="/user/logout">Logout</a></li>

                                </ul>
                            </li>

                        </ul>
                        <a class='menu-trigger'>
                            <span>Menu</span>
                        </a>

                    </nav>
                </div>
            </div>
        </div>
    </header>
    <!-- ***** Header Area End ***** -->
    <!-- ***** Main Banner Area Start ***** -->
    <div class="page-heading" id="top">
        <div class="container">
            <div class="row">
                <div class="col-lg-12">
                    <div class="inner-content">
                    </div>
                </div>
            </div>
        </div>
    </div>
    <!-- ***** Main Banner Area End ***** -->


    <!-- ------------------breadcrumbs starts----------------- -->



    <div class="container">
        <ol class="breadcrumb breadcrumb-arrow">
            <li><a href="/user/home">Home</a></li>
            <li><a href="/user/category?id=<%= product.category_id._id %>">
                    <%= product.category_id.category_name %>
                </a></li>
            <li class="active"><span>
                    <%= product.product_name %>
                </span></li>
        </ol>
    </div>
    <!-- ------------------breadcrumbs ends----------------- -->

    <!-- ***** Product Area Starts ***** -->
    <section class="section" id="product">
        <div class="container">
            <div class="row">
                <div class="col-lg-8">
                    <div class="row">
                        <div class="col-lg-6">
                            <div class="left-images">
                                <div class="image-container1">
                                    <img src="/<%= variant.images[0] %>" alt="Product Image 1" class="productImage"
                                        id="productImage1">
                                </div>
                                <div class="image-container2">
                                    <img src="/<%=variant.images[1] %>" alt="Product image 2" class="productImage"
                                        id="productImage2">
                                </div>
                            </div>
                        </div>
                        <div class="col-lg-6">
                            <div class="left-images">
                                <div class="image-container3">
                                    <img src="/<%= variant.images[2] %>" id="productImage3" class="productImage" alt="">
                                </div>
                                <div class="image-container4">
                                    <img src="/<%= variant.images[3] %>" alt="" class="productImage" id="productImage4">
                                </div>



                            </div>
                        </div>

                    </div>

                </div>
                <div class="col-lg-4">
                    <div class="right-content">
                        <h3>
                            <%= product.product_name %>
                        </h3>
                        <div class="mb-3">
                            <span class="badge bg-warning text-dark">4.5 ★</span>
                            <span>(150 Reviews)</span>
                        </div>
                        <% if (variant.offer) { %>
                            <button class="btn btn-outline-secondary btn-sm" style="text-decoration: line-through;" disabled>INR <%= variant.price %></button>
                            <button class="btn btn-outline-danger" disabled>INR <%=variant.discount_price %></button>
                            <button class="btn btn-danger " style="border-radius: 50%;" disabled><%= variant.offer.offerPercentage %>% off</button>
                              
                            
                          <% } else { %>
                            <h4 class="text-primary fw-bold">
                              INR <%= variant.price %>
                            </h4>
                          <% } %>
                          
                       
                        
                        <p>
                            <%= product.product_highlights %>
                        </p>

                        <div class="mb-3">
                            <label for="colors" class="form-label">Colour: <%= variant.color %></label>

                        </div>





                        <div class="mb-3">
                            <label for="colors" class="form-label">Size:
                                <button class="btn btn-outline-primary" disabled>
                                    <%= variant.size %>
                                </button>
                            </label>
                        </div>









                        <h5 class="<%= variant.stock > 0 ? 'text-success' : 'text-danger' %>">
                            <%= variant.stock> 0 ? `${variant.stock} In Stock` : 'Out of Stock'
                                %>
                        </h5>


                        <div class="variant-selector mb-3">
                            <h5 class="mt-4">Select Variant</h5>
                            <div class="row mt-4">
                                <% product.variants.forEach((variant, index)=> { %>
                                    <div class="col-3">
                                        <a href="/user/product?proId=<%= product._id %>&varId=<%= variant._id %>">
                                            <img class="w-75 variant-image" data-index="<%= index %>"
                                                src="/<%= variant.images[0] %>" alt="">
                                        </a>

                                    </div>
                                    <% }); %>
                            </div>
                        </div>
                        <div class="mb-3 mt-3 ">
                            <label for="quantity" class="form-label">Quantity:</label>
                            <input type="number" id="quantity" class="form-control" value="1" min="1"
                                max="<%= variant.stock %>">
                        </div>

                        <div class="mb-3">
                            <% if(variant.stock>0 ){ %>
                                <div class="row">
                                    <div class="col-md-10">
                                        <button class="btn btn-outline-dark w-100"
                                            onclick="addToCart('<%= product._id %>','<%= variant._id %>')">Add to
                                            cart</button>
                                    </div>
                                    <% if(ProductInWishlist) { %>
                                        <div class="col-md-2">
                                            <button type="button" onclick="addToWishlist('<%= product._id %>','<%= variant._id %>')" class="btn btn-outline-danger"><i class="bi bi-heart-fill"></i></button>
                                        </div>
                                    <% }else{ %>
                                            <div class="col-md-2">
                                                <button type="button" onclick="addToWishlist('<%= product._id %>','<%= variant._id %>')" class="btn btn-outline-dark"><i class="bi bi-heart"></i></button>
                                            </div>
                                    <% } %>

                                    
                                </div>


                                <% }else{ %>

                                    <button class="btn btn-danger w-100 mt-3" disabled>Notify me</button>
                                    <% } %>
                        </div>

                        <div class="d-flex justify-content-between">
                            <div>
                                <span class="d-block"><strong>Free Delivery</strong></span>
                                <small>Enter your postal code for delivery availability</small>
                            </div>
                            <div>
                                <span class="d-block"><strong>Return Delivery</strong></span>
                                <small>Free 30 Days Delivery Returns. Details</small>
                            </div>
                        </div>

                        
                    </div>
                </div>
            </div>
    </section>
    <!-- ***** Product Area Ends ***** -->


    <!-- Related Items -->
    <div class="section-heading mt-5">
        <h2 class="text-center">Related Products</h2>
    </div>
    <div class="container d-flex">
        <div class="row mt-5 d-flex">
            <% relatableProduct.forEach((element)=>{ %>
                <% if(element.product_name !=product.product_name){ %>
                    <div class="col-md-3 mt-4">
                        <div class="card" style="height: 70vh;">
                            <a href="/user/product?proId=<%= element._id %>&varId=<%= element.variants[0]._id %>"><img
                                    src="/<%= element.variants[0].images[0] %>" class="card-img-top"
                                    style="height: 50vh; object-fit: cover;" alt="Related Item 1"></a>
                            <div class="card-body text-center">
                                <h5 class="card-title">
                                    <%= element.product_name %>
                                </h5>
                                <p class="card-text text-danger"><small><del>
                                            <%= element.variants[0].price+120 %>
                                        </del></small>
                                    <%= element.variants[0].price %>
                                </p>
                                <span class="badge bg-warning text-dark">4.5 ★ (88)</span>
                            </div>
                        </div>
                    </div>
                    <% } %>
                        <% }) %>
        </div>
    </div>
    </div>
    </div>
    </div>

    </div>
    </section>

    <!-- ***** Footer Start ***** -->
    <footer>
        <div class="container">
            <div class="row">
                <div class="col-lg-3">
                    <div class="first-item">
                        <div class="logo">
                            <img src="/images/SnapCart.png" alt="hexashop ecommerce templatemo">
                        </div>
                        <ul>

                            <li><a href="#">SnapCart@company.com</a></li>
                            <li><a href="#">+91 9048763044</a></li>
                        </ul>
                    </div>
                </div>
                <div class="col-lg-3">
                    <h4>Shopping &amp; Categories</h4>
                    <ul>
                        <li><a href="#">Men’s Shopping</a></li>
                        <li><a href="#">Women’s Shopping</a></li>
                        <li><a href="#">Kid's Shopping</a></li>
                    </ul>
                </div>
                <div class="col-lg-3">
                    <h4>Useful Links</h4>
                    <ul>
                        <li><a href="#">Homepage</a></li>
                        <li><a href="#">About Us</a></li>
                        <li><a href="#">Help</a></li>
                        <li><a href="#">Contact Us</a></li>
                    </ul>
                </div>
                <div class="col-lg-3">
                    <h4>Help &amp; Information</h4>
                    <ul>
                        <li><a href="#">Help</a></li>
                        <li><a href="#">FAQ's</a></li>
                        <li><a href="#">Shipping</a></li>
                        <li><a href="#">Tracking ID</a></li>
                    </ul>
                </div>

            </div>
        </div>
    </footer>




    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>

    <script>
        async function addToCart(product_id, variant_id) {
            const quantity = document.getElementById('quantity').value
            if(quantity <=0){
                Swal.fire({
                    icon:'error',
                    title:'Invalid quantity',
                    text:'quantity must need more than zero',
                    confirmButtonText:'OK'
                })
                return 
            }
            try {
                const response = await fetch(`/cart/addToCart`, {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json'
                    },
                    body: JSON.stringify({ product_id, variant_id, quantity })
                });

                const result = await response.json();

                if (result.success) {
                    Swal.fire({
                        icon: 'success',
                        title: 'Added to cart',
                        showConfirmButton: false,
                        text: result.message,
                        timer: 1500
                    }).then(() => {
                        location.reload();
                    });
                } else {
                    Swal.fire({
                        icon: 'error',
                        title: 'Oops...',
                        text: result.message,
                        showConfirmButton: true
                    }).then(() => {
                        location.reload();
                    });
                }
            } catch (error) {
                Swal.fire({
                    icon: 'error',
                    title: 'Error',
                    text: 'An error occurred while processing your request.',
                    showConfirmButton: true
                });
            }
        }

        async function addToWishlist(productId,variantId){
            
            try{
                const response = await fetch(`/wishlist/toWishlist`,{
                    method:"POST",
                    headers:{
                        'Content-Type':'application/json'
                    },
                    body:JSON.stringify({productId,variantId})
                })
                const result = await response.json()
                console.log(result)
                if(result.success){
                    Swal.fire({
                        icon: 'success',
                        title: 'Added to wishlist',
                        showConfirmButton: true,
                        text: result.message,
                    }).then(() => {
                        location.reload();
                    });
                }else{
                    if(result.error == "product already in  wishlist"){
                        Swal.fire({
                        icon: 'info',
                        title: 'Already Added',
                        text: result.error,
                        showConfirmButton: true
                    })
                    }else{
                        Swal.fire({
                        icon: 'error',
                        title: 'Oops...',
                        text: result.error,
                        showConfirmButton: true
                    })
                    }
                   
                }

            }catch(error){
                Swal.fire({
                    icon: 'error',
                    title: 'Error',
                    text: 'An error occurred while processing your request.',
                    showConfirmButton: true
                });
            }
        }


        document.addEventListener('DOMContentLoaded', () => {
            const imageContainers = document.querySelectorAll('.image-container1, .image-container2, .image-container3, .image-container4');
            imageContainers.forEach(container => {
                const productImage = container.querySelector('.productImage');

                container.addEventListener('mousemove', function (e) {
                    const rect = container.getBoundingClientRect();
                    const x = e.clientX - rect.left;
                    const y = e.clientY - rect.top;
                    productImage.style.transformOrigin = `${x}px ${y}px`;
                });

                container.addEventListener('mouseleave', function () {
                    productImage.style.transformOrigin = 'center';
                });
            });
        });


    </script>



    <%- include ('../partial/footer')-%>